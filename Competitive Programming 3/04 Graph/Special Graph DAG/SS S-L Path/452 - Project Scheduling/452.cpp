#include <bits/stdc++.h>
using namespace std;
#define forr(i,a,b) for(int i=(a); i<(b); i++)
#define forn(i,n) forr(i,0,n)

int T,a,b,w;
char cu,cv;


void dfs(int u){
	
	
	
	
}


int main(){
	
	scanf("%d",&T);
	while(T){
		int cantn=1;
		while(scanf("%c %d %c",&cv,&w,&cu)==3){
			if(m.count(cv)==0)m[cv]=cantn++;
			if(m.count(cv)==0)m[cu]=cantn++;
			g[m.[cu]].push_back(m[cv]);
			
		}
		T--;
		else{
			
		}
	}
	
	
	
	
	return 0;
}
