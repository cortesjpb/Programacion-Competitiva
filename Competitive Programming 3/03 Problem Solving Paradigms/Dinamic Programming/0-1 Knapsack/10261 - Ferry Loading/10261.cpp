#include <bits/stdc++.h>
using namespace std;

int T, N, W;
int dp[105][10005][3];
int w[105];

int state(int i, int x, int l){
	
	
	
}


int main() {
	cin >> T;
	while(T--){
		cin >> W;
		N=0;
		while(true){
			cin >> w[N];
			N++;
			if(w[N]==0) break;
		}
		memset(dp, -1, sizeof dp);
		
	}
	
	
	return 0;
}
